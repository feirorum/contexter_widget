This is a system for showing context to what the user is doing. 
The context could be a current active window or selected text.

A flexible system of actions on selected text should allow for both lookups of information 
and storing the selected text in various ways.

